# EventLoop

JS is synchronous and single threded bydefault

## There can be async behaviour 
- with BrowserAPI -
setTimeout, setInterval, setImmediate, nextTick
- with promises
- with event handdlers


A function not executed , but it must be executed afterr a while 
# Call Back
- That pass as argument or the parameter to another function

# Mordern java script is divided in 2 parts 
1. CommanJS (cjs) -> supports OOPS -> require
    - priority (nextTrick , promise , setImmediate/setTimeout)
2. ModuleJS (.mjs) -> follow modular approach -> import
    - priority 


rightFile , apendFile 
all fun are promise , so it must be called with await    